import withStyles from "@mui/styles/withStyles";

const styles = (theme) => ({
    menuLink: {
        textDecoration: 'none',
        color: 'inherit'
    }
});

const HomePage = () => {
    return (
        <div></div>
    );
};

export default withStyles(styles)(HomePage);